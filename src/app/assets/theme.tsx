import { createTheme } from "@mui/material"

const theme = createTheme({
    palette: {
        mode: 'dark',
        background: {
            default: '#242424'
        },
        primary: {
            main: '#8b0000'
        },
    },
})

export default theme